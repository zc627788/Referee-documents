# 裁判文书角色提取项目 · 完整分析 & GLM-4 费用估算

## 一、项目架构总览

这是一个从中国裁判文书 CSV 数据中提取**审判人员信息**（审判长、审判员、书记员等角色）的两阶段 NLP 流水线。

```
输入 CSV (全文 + 案号)
       │
  ┌────▼──────────┐
  │  Phase 1      │  规则提取（纯本地，免费）
  │  RuleExtractor│  全文扫描 → 边界字符判断
  └────┬──────────┘
       │
  ┌────▼──────────┐        ┌──────────────────┐
  │ 确定 (~70%)   │        │ 不确定 (~30%)     │
  │ 直接写 CSV    │        │ 进入 ai_queue.csv │
  └───────────────┘        └────────┬─────────┘
                                    │
                           ┌────────▼─────────┐
                           │  Phase 2          │  GLM-4 AI 精修
                           │  批量打包 ≤500条   │  (产生费用)
                           │  并发10线程        │
                           └────────┬─────────┘
                                    │
                           最终合并 result.csv
```

## 二、核心模块说明

| 模块 | 文件 | 功能 |
|------|------|------|
| [RuleExtractor](file:///e:/Referee-documents/src/core/rule_extractor.py#170-429) | [src/core/rule_extractor.py](file:///e:/Referee-documents/src/core/rule_extractor.py) | 全文扫描，用边界字符判断确定性 |
| [run_pipeline.py](file:///e:/Referee-documents/src/run_pipeline.py) | [src/run_pipeline.py](file:///e:/Referee-documents/src/run_pipeline.py) | 主流控：phase1/phase2/retry/status |
| `ProgressDB` | [src/core/progress_db.py](file:///e:/Referee-documents/src/core/progress_db.py) | SQLite 记录进度，支持断点续跑 |
| `Config` | [src/core/config.py](file:///e:/Referee-documents/src/core/config.py) | 读 config.json |
| [_ai_batch_extract](file:///e:/Referee-documents/src/run_pipeline.py#72-194) | `run_pipeline.py:72` | GLM-4 批量调用 + 并发 |

### Phase 1（规则提取，免费）
1. 逐行读取 CSV `全文` 字段（chunk=5000 行/批）
2. 对每篇文书全文扫描 **24种角色关键词**（审判长/审判员/书记员等）
3. 角色词后紧跟**边界字符**（标点/换行/数字年份）→ **确定命中**，直接写结果
4. 角色词后紧跟**中文字符** → **不确定**，放入 `ai_queue.csv`
5. 无角色词 → 写异常记录

### Phase 2（AI精修，产生费用）
- 读取 `ai_queue.csv` 中全部**待处理**片段
- 每个送 AI 的片段 = `角色词 + 前5字上下文 + 后30字`，约 **40~60字/片段**
- 按 `MAX_INPUT_TOKENS=80000` 上限打包，**每包最多 500 条**
- 送 GLM-4：判断是否有效人名，返回 JSON 数组
- 并发度：10（config.json）

---

## 三、数据规模分析（实际采样 300 条）

| 指标 | 数值 |
|------|------|
| 全文均值字数 | ~2,188 字 |
| 全文中位数字数 | ~777 字 |
| 全文最大字数 | ~14,206 字 |
| 全文Token均值（估算）| ~3,749 tokens |
| 全文Token中位数 | ~1,358 tokens |

> Token估算公式（同代码逻辑）：中文字 × 2 + 英文/数字 × 0.5

---

## 四、GLM-4 费用估算（60万条数据）

### 定价
| 类型 | 单价 |
|------|------|
| 输入 token | ¥0.1800 / 1M tokens |
| 输出 token | ¥0.7200 / 1M tokens |

---

### 关键前提：Phase 2 的触发比例

Phase 1 已经用规则处理大部分文书，**只有"不确定片段"才进入 AI 队列**。
根据代码逻辑，AI 片段的触发条件相对严格，估计：

| 场景 | AI队列触发率 | 说明 |
|------|-------------|------|
| 乐观 | 10% | 文书格式规范，落款清晰 |
| **中等（推荐）** | **25%** | **历史实测常见水平** |
| 悲观 | 40% | 文书格式混乱，大量粘连文本 |

每条 AI 片段发送的内容≈ **角色词(3~5字) + 上下文(35字) = ~40字 ≈ 80 tokens**

### Phase 2 输入Token计算

每个 API 请求包含：
- **系统Prompt 固定开销**：约 200 tokens（中文提示语 ~100字）
- **每条片段**：约 80 tokens（片段文本 + ID + 角色词格式）
- 每包500条 → 输入：`200 + 500 × 80 = 40,200 tokens/包`

每个 API 请求输出 token：
- 每条返回 `{"id":"...","name":"张三"}` ≈ **25 tokens**
- 500条/包 → 输出：`500 × 25 = 12,500 tokens/包`

---

### 💰 费用汇总表

| 场景 | AI处理片段数 | 输入Token | 输出Token | 输入费用 | 输出费用 | **总费用** |
|------|------------|-----------|-----------|---------|---------|-----------|
| 乐观（10%） | 60,000 条 | 4.8M | 1.5M | ¥0.86 | ¥1.08 | **≈ ¥2** |
| **中等（25%）** | **150,000 条** | **12M** | **3.75M** | **¥2.16** | **¥2.70** | **≈ ¥5** |
| 悲观（40%） | 240,000 条 | 19.2M | 6M | ¥3.46 | ¥4.32 | **≈ ¥8** |

> Phase 1 本地规则处理：**¥0（完全免费）**

---

### ⚠️ 重要补充说明

1. **Phase 1 本身不消耗 API**：60万条文书的全文解析是纯本地运算，不花钱。GLM-4 费用只来自 Phase 2 的"不确定片段"处理。

2. **片段 ≠ 文书**：AI 队列里每条是一个短片段（约40字），不是发完整全文。这是成本低的关键原因。

3. **实际费用极低**：即使悲观估计也只有 **¥8 左右**，远比发整篇全文便宜。

4. **如果改成发完整全文给AI**（非当前架构），60万条×中位1358 tokens = 814M tokens，仅输入就要 **¥146** 以上，输出更高。当前架构已经做了很好的优化。

5. **并发设置**（concurrency=10）：不影响总 token 数，只影响速度。

---

### 📊 时间估算（Phase 2）

- 每个请求约耗时 5~15 秒（GLM-4）
- 中等场景：150,000 条 / 500条每包 = 300 个请求
- 并发10线程：300 / 10 = 30 批次
- 预计**30批次 × 平均10秒 = 约5分钟**完成 Phase 2

---

## 五、结论

| | 数值 |
|---|---|
| **Phase 1（规则提取）费用** | **¥0** |
| **Phase 2（AI精修）费用 · 乐观** | **~¥2** |
| **Phase 2（AI精修）费用 · 中等** | **~¥5** |
| **Phase 2（AI精修）费用 · 悲观** | **~¥8** |
| **Phase 2 预计运行时间** | ~5~15 分钟 |

> **结论：处理 60 万条数据，GLM-4 总花费预计在 ¥2 ~ ¥8 之间，性价比极高。**
